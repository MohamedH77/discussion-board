<div class="row">
  <div class="col-3">
    {{#each posts as |post| }}
    <div class="flex-column flex-wrap w-100">
      <div class="display-flex">
        <div class="">
          <div class="card p-4 card-rounded">
            <div class="card card-header">
              <h2>Author: {{post.User.fname}} {{post.User.lname}}</h2>
              <h2>{{post.title}} </h2>
            </div>
            <div class="card card-body card-rounded">
              <p>{{post.body}}</p>

            </div>
            <div class="card card-body card-rounded">
              <p>Posted On: {{post.createdAt}}</p>
            </div>
            <div class="card card-footer">
              <h3><button data-id={{post.id}} class="btn btn-primary w-100 commentsTag">Show Comments</button></h3>
            </div>
          </div>
        </div>
      </div>
    </div>
    {{/each}}
  </div>

  <div class="col-xl-9">
    <div class="">
      <div class="display-flex">
        <div class="w-100">
          <div class="card p-4 card-rounded">
            <div class="card card-header">
              <h2>Author: {{selectedPost.User.fname}} {{selectedPost.User.lname}}</h2>
              <h2>{{selectedPost.title}} </h2>
            </div>
            <div class="card card-body card-rounded">
              <p>{{selectedPost.body}}</p>

            </div>
            <div class="card card-body card-rounded">
              <p>Posted On: {{selectedPost.createdAt}}</p>
            </div>
            <div class="card card-footer card-rounded">
              <h3 data-id={{selectedPost.id}} class="w-100 commentsTag">Comments</h3>
              <div class="card card-footer card-rounded">
                <p>User Id:</p>
                <p>Comment:</p>
                <p>Upvote/Downvote buttons</p>
              </div>
            </div>
            <div class="card card-footer card-rounded">
              <form class="form login-form">
                <div class="form-group">
                  <label for="password-login">Comment:</label>
                  <input class="form-input" type="comment" id="comment" placeholder="comment text goes here" />
                </div>
                <div class="form-group">
                  <button class="btn btn-primary" type="submit">Comment</button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>